const Header = () => {
    const headerStyle = {
    backgroundColor : "lightblue",
    color: "#fff"
    };

return(
    <header>
        <h1>Groceries List</h1>
    </header>
    )
}

export default Header