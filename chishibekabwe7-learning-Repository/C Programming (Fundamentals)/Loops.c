#include <stdio.h>
int main (){
    //While loop
    int i = 0;
    while(i<10){
        printf("%d\n",i);
        i++;
    }
    
    //For loop
    int iterator;
    for(iterator = 10; iterator < 21; iterator++){
        printf("%i\n",iterator);
    }

    return 0;
}