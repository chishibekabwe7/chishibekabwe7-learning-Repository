#include <stdio.h>

    //Declaring a function
    void myfunction () {
        printf("My first C Programming function to executed!!");
    }
    //Note functions are always declared outside the main function

int main (){
    //Calling the function
    myfunction();

    return 0;
}