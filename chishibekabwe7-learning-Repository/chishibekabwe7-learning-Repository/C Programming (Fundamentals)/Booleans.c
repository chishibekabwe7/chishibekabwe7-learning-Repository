#include <stdio.h>
#include <stdbool.h> //Don't forget to add this preprocessor or else the booleans wont run

int main (){
bool isProgrammingFun = true;
bool isProgrammingBoring = false;

printf("%d\n",isProgrammingFun);    //outputs 1 if its true
printf("%d\n",isProgrammingBoring); //outputs 0 if its false 
    return 0;
}
